* For what is the folder

This directory should contain all specific identifications of users classes.

This classes creates en instance of admin,guest users classes and initializes class members for necessary validation. 
 
* What kind of files can be placed there

 This files is a template for authorized user classes, for example authorized admin,guest users in the project. 

* File system access rights

  Read only by php.